# Ema Dev Editor Extension

This extension is a VSCode extension for the Ema Dev project.

## Installation

```shell
pnpm package && pnpm vsc:install
```

## Features

- Syntax highlighting for MD strings in JavaScript, TypeScript, JSX, and TSX.

  To get a `md` impl, see [utils.ts](/packages/ema/src/utils.ts)
